# Shared contracts

This directory contains only language-neutral API contracts, schemas and test
fixtures. React and Flutter own their generated clients, state and UI code.

- `contracts/openapi.yaml`: additive `/api/v1` contract.
- `schemas/`: shared enum and validation definitions.
- `fixtures/`: contract-test examples without real contact information.
